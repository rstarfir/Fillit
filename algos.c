pseudo algos
int		fillit()

while (!solvemap)
	{
		while (overmap x)
		{
			while (overmap y)
			{
				if(!overlap)
					fillthemap( map, tmp, tmp->alpha)
					if (fillit())
						return(1);
					else
						fillthemap(map, tmp, ".")
				Xoffset++;					
			}
			Xoffset = 0;
			Yoffset++;		
		}
		return (0);
		free_map;
		mapsize(size + 1);		
	}
printmap;
freemap;